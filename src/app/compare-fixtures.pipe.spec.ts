import { CompareFixturesPipe } from './compare-fixtures.pipe';

describe('CompareFixturesPipe', () => {
  it('create an instance', () => {
    const pipe = new CompareFixturesPipe();
    expect(pipe).toBeTruthy();
  });
});
